<%= form_for(@cable_type) do |f| %>
  <% if @cable_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cable_type.errors.count, "error") %> prohibited this cable_type from being saved:</h2>

      <ul>
      <% @cable_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sku %><br>
    <%= f.text_field :sku %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :grams_per_metre %><br>
    <%= f.number_field :grams_per_metre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
